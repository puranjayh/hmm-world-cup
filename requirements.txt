# Core — required to train, evaluate, and predict
hmmlearn>=0.3
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
xgboost>=2.0

# Tournament simulator (model/gaussian_hmm/wc2026simulator.py)
tabulate>=0.9

# Data collection only — not needed to reproduce the published results
requests>=2.31          # data/raw/update_elo.py
soccerdata>=1.8         # scripts/soccer_data.py
